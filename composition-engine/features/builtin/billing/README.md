# Billing Feature

SaaS billing integration supporting Stripe checkouts, subscriptions, and webhook events.

## Requirements

- Requires: `organizations` (and transitively `authentication` and `database`)
