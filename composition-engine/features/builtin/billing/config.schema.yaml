type: object
properties:
  provider:
    type: string
    enum: [stripe, paddle, braintree]
    default: stripe
    description: "SaaS payment provider integration"
  currency:
    type: string
    default: USD
    description: "System currency handle"
required: []
