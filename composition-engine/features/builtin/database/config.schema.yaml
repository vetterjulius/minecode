type: object
properties:
  connectionString:
    type: string
    description: "PostgreSQL connection string URL"
  pooling:
    type: boolean
    default: true
    description: "Enable connection pooling"
required: []
